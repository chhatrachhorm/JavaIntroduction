
public class constantExample {

	public static void main(String[] args) {
		/*
		 * Predefined Constant
		 * */
		System.out.println(Math.PI);
		System.out.println(Integer.MAX_VALUE);
		/*
		 * We can access function and other non-static variables unless we crate an object for the variable
		 * But for static variable, we can access it through class name(because static variable can associate 8
		 * */
	}

}
